# Creator UI model

Framework-neutral state for the reference Studio: catalog discovery, compatibility-safe equip operations, undo/redo, deterministic randomization, preview state, and portable recipe import/export. Hosts subscribe to snapshots; the package has no DOM dependency.
